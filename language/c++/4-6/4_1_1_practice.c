#include <stdio.h>
int main(){

    int a=0,n;
   
    printf("Enter the n term: ");
    scanf("%d",&n);

    //Till here i took the user input now internal work by for loop

    for(a ; n>a ; a++ )
    
    printf("%d\n",a);

    //An interseting thing if you enter value in millions what happen?
    


    return 0;
}