#include <stdio.h>
int main(){
 //While loop only work if condition is true  end stop when it become false
//  you need to add these curly brckets {} otherwise program fails

    int a=1; 
    while(a<12)
         {
         printf("%d\n",a);
         a++;
         //Or i can also writ as   a=a+1
         }
    return 0;
}