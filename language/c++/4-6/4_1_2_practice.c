#include <stdio.h>
int main(){

    int n;
    printf("Enter n: ");
    scanf("%d",&n);

    for(n;n;n--)
    //This code executed till n is not zero so first n for location 
    //second for determination(Till wher to do)it also decrementuntil n reaches 0
    
    printf("%d\n",n);

    return 0;
}